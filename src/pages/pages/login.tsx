import PageLayout from '@/components/layout/page-layout';
import LoginPage from '@/views/login-page';

const LoginPagePage = () => {
  return (
    <PageLayout pageTitle="Login Page">
      <LoginPage />
    </PageLayout>
  );
};

export default LoginPagePage;
